{
  "name": "overdoo",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter ./web dev",
    "build": "pnpm --filter ./web build",
    "test:contracts": "cd contracts && forge test",
    "find-delayed": "pnpm --filter ./web find-delayed"
  }
}
